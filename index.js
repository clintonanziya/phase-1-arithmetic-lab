let num1 = 31;
let num2 = 2;
let num3 = 14;
let num4 = 5;

let multiply = num1 * num2; // Result should be 62

let random = Math.floor(Math.random() * Number.MAX_SAFE_INTEGER) + 1; // Generates a random integer greater than 0

let mod = num3 % num4; // Remainder of dividing num3 by num4 should be 4

let max = Math.max(2, 4, 6, 8, 10, 20); // Finds the highest number in the set, value returned should be 20
